{
  "name": "linkedin-pinpoint-frontend-astro",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/fetch-data.js && astro build",
    "build-only": "astro build",
    "preview": "astro preview",
    "fetch-data": "node scripts/fetch-data.js"
  },
  "dependencies": {
    "@astrojs/svelte": "^8.1.1",
    "astro": "^6.0.0",
    "marked": "^12.0.0",
    "svelte": "^5.0.0"
  }
}
